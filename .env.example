# ASP.NET Core Configuration
ASPNETCORE_ENVIRONMENT=Development

# API Configuration
API_PORT=5000

# PostgreSQL Configuration
POSTGRES_USER=roundsuser
POSTGRES_PASSWORD=roundspassword
POSTGRES_DB=roundsdb
POSTGRES_PORT=5432

# JWT Configuration (Generate a secure random key for production!)
JWT_SECRET_KEY=your-secret-key-minimum-32-characters-long-change-in-production
JWT_ISSUER=RoundsAPI
JWT_AUDIENCE=RoundsApp
JWT_EXPIRY_MINUTES=60
